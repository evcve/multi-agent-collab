# 多智能体协作模式协议 (Collaboration Protocol)

> 实战验证的协作模式：BRAIN（主控）— HANDS（编排 agent）— WORKERS（子模型）三层架构。
> 本协议目标是：**让执行者一次做对**，让**幻觉无处遁形**，让**成本可控**。

## 1. 分工矩阵

| 角色 | 职责 | 明确不做 |
|---|---|---|
| BRAIN（主控） | 意图拆解、记忆/知识管理、任务派发、结果核验、人类接口 | 重复执行已派发的机械任务 |
| HANDS（编排 agent） | 多步推理、工具编排、复杂任务执行 | 单轮简单问答（浪费编排能力） |
| WORKERS（子模型） | 单轮生成、简单验证、格式化输出 | 需要工具链/文件系统访问的任务 |

**黄金准则**：任务按"所需能力"分配，不是按"谁有空"分配。简单任务给免费层，复杂任务给编排层。

## 2. 任务规范：四要素

每条派发任务**必须**包含：

1. **目标** — 一句话说清要什么结果（可验收）
2. **上下文** — 关键数据**内嵌任务文本**，禁止"请读取文件 X"式指令
   > ⚠️ 实战教训：执行层无文件能力时，"读文件"指令必然被模型幻觉填补——它假装读了然后编造内容。**数据内嵌是唯一可靠的传递方式**（短数据直接内嵌；长数据先由脚本读入再拼进 prompt）。
3. **约束** — 边界条件、禁止事项、输出格式（如"≤400 字要点式"）
4. **验收标准** — 可检查的交付物：文件路径 / 数值范围 / 格式规范

## 3. 反馈协议：三态结构化

执行结果必须返回结构化状态，取代自由文本汇报：

| 状态 | 含义 | 必须附带 |
|---|---|---|
| `done` | 完成 | **可验证的路径/数值**（接收方可复查） |
| `failed` | 失败 | 失败原因（不掩盖、不美化） |
| `need_confirm` | 需决策 | 选项列表 + 建议 |

**防幻觉纪律**：
- 不确定就说不知道——**绝不编造**结果、文件、数值
- 声称"已写入/已上传"必须给出手柄（文件路径/URL），接收方**必须验证**
- 涉及关键数值（工程/财务/医疗）一律回源核验，不轻信子代理自报

## 4. 迭代机制：心跳与中断

- **心跳**：长任务分阶段回报进度（如"已完成 2/5 步"），人类/主控可中途介入
- **中断**：任务方向偏差时，主控可撤回、改派、补充指令——不用等任务失败
- **优先级**：人类直接指令 > 紧急任务插队 > 常规队列

## 5. 成本治理：模型分级

```
简单验证/单轮生成  → 免费层（便宜/快，量大不心疼）
中等文本任务       → 主力层（平衡质量与成本）
视觉/复杂推理      → 专项层（能力最强，最贵，按需用）
```

- 配额告警/耗尽时**自动降级**到下一层，不让任务卡死
- 长任务**分块执行**，每块只传必要上下文（防记忆溢出，也省钱）
- 简单任务用免费模型是省钱，复杂任务硬用免费模型是浪费——分级要动态判断

## 6. 固化纪律（从实战教训提炼）

1. **文件操作归脚本，LLM 只做推理** — 脚本负责 IO，模型只回答问题（零幻觉面）
2. **改配置先备份**（`.bak.YYYYMMDD`）
3. **长文本/大代码生成**要调大 `max_tokens`（默认 2000 会截断输出）
4. **LLM 无文件能力时**，任务要求它"输出 HTML 代码"，由**调用方**保存文件——模型别碰文件系统
5. **协作协议本身要迭代**：每次踩坑就补一条，文档跟着实战走

## 7. 安全底线

- 凭据（API key/token/密码）**永不进入任务文本**、永不提交到仓库
- 发布共享内容前做脱敏检查：内部数值、配额、账号、路径全部泛化
- 模型调用凭据走环境变量/独立文件，不进代码

---
*本协议由实战项目迭代而来，欢迎 issue/PR 补充新教训。*
